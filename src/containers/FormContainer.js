import React from 'react';

export default function FormContainer(props) {
    const { form } = props

    return (
        <div className='form-container'>
            Form to login signup
        </div>
    )
}